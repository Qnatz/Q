Your final output MUST be a JSON object with two keys: "route" and "message".
- The "route" must be one of the available `{ROUTING_OPTIONS}`.
- The "message" should be your concise response to the user.